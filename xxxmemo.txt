作業開始時

clasp push --watch

（止めるときは Ctrl + C）

ブラウザ側を触ったあと

clasp pull


関数を直接実行したいとき

clasp run 関数名


普段の編集では
👉 ターミナルは横に置いておくだけ。

------------------------------------------------------

VS Codeから特定のGAS関数を実行する＝clasp run。これが王道。

いちばん基本（1回だけ実行）

ターミナル（VS Code内でOK）で：

clasp run 関数名

------------------------------------------------------

ログ確認（実行後）
clasp logs


リアルタイムで見たいなら：

clasp logs --watch

------------------------------------------------------
